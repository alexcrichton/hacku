YUI().use('anim', 'node', function(Y){
  Y.one('body').append("<%= escape_javascript render('canvas') %>");
  Y.one('#o3d').setStyles({
    width: '98%',
    height: '500px'
  });

  jQuery('#form, h1').slideUp(function() {
    jQuery(this).remove();
  });

  initClient(<%= raw @output %>);
});
